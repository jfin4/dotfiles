#!/usr/bin/env zsh

for dir in notes projects records resources; do
    echo
    echo $dir ------------------------------------------------------------------------------
    rclone --progress copy ~/$dir docs:$dir
done

for dir in resources; do
    echo
    echo $dir ------------------------------------------------------------------------------
    rclone --progress copy ~/$dir data:$dir
done

#!/usr/bin/env zsh

for dir in notes projects records resources; do
    echo
    echo $dir ------------------------------------------------------------------------------
    rclone --progress copy docs:$dir ~/$dir 
done

for dir in resources; do
    echo
    echo $dir ------------------------------------------------------------------------------
    rclone --progress copy data:$dir ~/$dir 
done

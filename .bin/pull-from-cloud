#!/usr/bin/env zsh

for dir in notes projects records resources; do
    echo
    echo $dir ------------------------------------------------------------------------------
    rclone --progress copy ~/$dir docs:$dir
done

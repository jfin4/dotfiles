# /etc/keyd/default.conf

[ids]
*

[xwin:layout]
backspace = overload(meta, backspace)
delete = setlayout(tmux)

[tmux:layout]
backspace = overload(alt, backspace)
delete = setlayout(xwin)

[control]
backspace = backspace
delete = delete
space = setlayout(xwin)
enter = setlayout(main)
